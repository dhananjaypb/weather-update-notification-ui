import { NotificationDetails } from './notification-details';

describe('NotificationDetails', () => {
  it('should create an instance', () => {
    expect(new NotificationDetails()).toBeTruthy();
  });
});
